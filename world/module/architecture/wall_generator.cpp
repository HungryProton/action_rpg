#include "wall_generator.hpp"
#include "common/logger.hpp"

namespace game{

	std::vector<Wall> WallGenerator::Generate(double seed){
		std::vector<Wall> walls;
		LOG(DEBUG) << "Generating with seed " << seed << std::endl;

		return walls;
	}
}
